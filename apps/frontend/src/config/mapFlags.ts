export const MAP_FLAGS = {
  // Habilita animaciones globales en el mapa (mejora UX, impacta rendimiento)
  ENABLE_MAP_ANIMATIONS: true,

  // Umbral de entidades para desactivar efectos pesados automáticamente
  PERFORMANCE_THRESHOLD_HIGH: 500,

  // Renderizar usando Canvas en lugar de SVG cuando sea posible (mejor para muchos elementos)
  USE_CANVAS_RENDERER: true,
};
